# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its frontend apps focus without claiming live deployment or external usage.

## Cases

- `baseline`: `view drift`, score 153, lane `ship`
- `stress`: `state pressure`, score 208, lane `ship`
- `edge`: `layout risk`, score 164, lane `ship`
- `recovery`: `interaction cost`, score 223, lane `ship`
- `stale`: `view drift`, score 197, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
